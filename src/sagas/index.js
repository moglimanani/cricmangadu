import { takeLatest } from 'redux-saga/effects';
// import { getAllCategory } from './sagas/category';
// import { getAllProducts } from './sagas/product';
// import { addCart } from './sagas/cart';
// import CATEGORY_TYPES from './types/category';
// import PRODUCT_TYPES from './types/productTypes';
// import CART_TYPES from './types/cartTypes';

//import Api from '...



// Starts fetchUser on each dispatched USER_FETCH_REQUESTED action
// Allows concurrent fetches of user
function* mySaga() {
    //   yield takeLatest(CATEGORY_TYPES.GET_ALL_CATEGORY, getAllCategory);
    //   yield takeLatest(PRODUCT_TYPES.GET_ALL_PRODUCTS, getAllProducts);
    //   yield takeLatest(CART_TYPES.ADD_CART, addCart);
}

export default mySaga;